# Few-shot: обучение на примерах

import os
from typing import Any
from system_promt import SYSTEM_PROMPT

api_key_google = os.getenv("OPENROUTER_API_KEY_GOOGLE")
if not api_key_google:
    raise SystemExit("Не найден OPENROUTER_API_KEY_GOOGLE в переменных окружения или .env")

api_key = os.getenv("OPENROUTER_API_KEY")
if not api_key:
    raise SystemExit("Не найден OPENROUTER_API_KEY в переменных окружения или .env")

TEST_BUGS_JSON = """
[
    {"внутренний номер бага": "101", "Наименование": "Карты Лучи::Не работает интернет на оплаченных сим", "Описание": "Не работает интернет на оплаченных и активных сим Лучи", "Причина обращения": "Нет сети::Лучи", "статус": "В процессе обзора", "влияние бага": "Блокирует работу сети интернет", "дата": "2026-05-01"},
    {"внутренний номер бага": "102", "Наименование": "Эквайринг::Не проходит оплата с эквайрингом от Борис банка. Ошибка нет связи с сервером", "Описание": "На терминалах с эквайрингом от банка Борис не проходит оплата, при оплате возникает ошибка: нет связи с сервером", "Причина обращения": "Эквайринг::Ошибки оплаты", "статус": "Открыт", "влияние бага": "Блокирует работу с эквайрингомг", "дата": "2026-04-15"},
    {"внутренний номер бага": "103", "Наименование": "Core: Отсутствует флаг EXTERNAL_EXTERNAL при установке "РТР" при использовании тарифа ptp-6000", "Описание": "При установке РТР с тарифом ptp-6000 не отдаётся флаг EXTERNAL_EXTERNAL. После включения флага обращение не закрывать, т.к. после исправления потребуется отключать флаги отданные вручную иначе после отмены/истечении подписки смогут пользоваться PTP без оплаты", "Причина обращения": "Ошибки::другие ошибки, "статус": "Создана", "влияние бага": "Блокирует работу с документами", "дата": "2026-04-11"},
]
"""

def build_client() -> Any:
    try:
        from dotenv import load_dotenv
    except ImportError as exc:
        raise SystemExit(
            "Установите зависимость python-dotenv: pip install python-dotenv"
        ) from exc

    try:
        from openai import OpenAI
    except ImportError as exc:
        raise SystemExit("Установите зависимость openai: pip install openai") from exc

    load_dotenv()
    if not os.getenv("OPENROUTER_API_KEY"):
        raise SystemExit("Не найден OPENROUTER_API_KEY в переменных окружения или .env")

    return OpenAI(
    base_url="https://openrouter.ai/api/v1",  # Указываю путь к OpenRouter
    api_key=api_key,
    )


def build_messages() -> list[dict[str, str]]:
    """Few-shot классификатор тикетов поддержки."""
    return [
        {"role": "system", "content": SYSTEM_PROMPT},

        # 1. Позитивный типовой кейс (Баг найден, отсекаем шум)
        {
            "role": "user", 
            "content": f"База багов: {TEST_BUGS_JSON}\n\nЗапрос: стоит сим карта Лучи, проплачена на квартал, передача данных включена, показывает что сеть есть, но по факту пишет что отсутствует связь"
        },
        {
            "role": "assistant", 
            "content": "## Релевантные баги\n*   **[101]** — Сбой передачи данных сим-карт Лучи\n    *   *Статус:* В работе\n    *   *Причина:* Проблема с сим-картой Лучи"
        },

        # 2. Попытка запросить инструкцию при наличии бага. 
        # Модель должна проигнорировать просьбу об инструкции и выдать ТОЛЬКО классификацию бага
        {
            "role": "user", 
            "content": f"База багов: {TEST_BUGS_JSON}\n\nЗапрос: стоит сим карта Лучи, проплачена на квартал, передача данных включена, показывает что сеть есть, но по факту пишет что отсутствует связь. Дай пошаговую инструкцию."
        },
        {
            "role": "assistant", 
            "content": "## Релевантные баги\n*   **[101]** — Сбой передачи данных сим-карт Лучи\n    *   *Статус:* В работе\n    *   *Причина:* Проблема с сим-картой Лучи"
        },

        # 3. Пограничный кейс (Запрос про эевайринг, есть похожие слова, но смысл другой  - пограничный вариант)
        {
            "role": "user", 
            "content": f"База багов: {TEST_BUGS_JSON}\n\nЗапрос: Как подключить эквайринг от банка Борис? сейчас у меня выходит уведомление, что нужно оставить заявку и подключить эквайринг в банке, без этого не могу принимать оплату картами"
        },
        {
            "role": "assistant", 
            "content": "подходящих багов не найдено"
        },
        # 4. Пограничный кейс негативный кейс (Запрос про приложение PTP, есть похожие слова, но смысл другой  - у кл вопрос)
        {
            "role": "user", 
            "content": f"База багов: {TEST_BUGS_JSON}\n\nЗапрос: Какие тарифы есть у приложения РТР?"
        },
        {
            "role": "assistant", 
            "content": "подходящих багов не найдено"
        },

        # 5. Реальный запрос для проверки (Бот должен вернуть 'подходящих багов не найдено')
        {
            "role": "user", 
            "content": f"База багов: {TEST_BUGS_JSON}\n\nЗапрос: Хочу купить симку Лучи, но денег нет, теперь без интернета буду сидеть"
        }
    ]
    # Ожидаемый ответ: {"подходящих багов не найдено"}


def main() -> None:
    client = build_client()
    messages = build_messages()

    response = client.chat.completions.create(
        model="openai/gpt-oss-120b:free",
        messages=messages,
        temperature=0,
        max_tokens=1000,
    )

    print("Запрос: Хочу купить симку Лучи, но денег нет, теперь без интернета буду сидеть") # Проблемный 5 тест-кейс, где с промтом без фьюшотов модель ошиблась
    print(f"Ответ модели: {response.choices[0].message.content}")
    print("Ожидаемый:    подходящих багов не найдено")


if __name__ == "__main__":
    main()
    
# Ответ по коду:
#Запрос: Хочу купить симку Лучи, но денег нет, теперь без интернета буду сидеть
#Ответ модели: подходящих багов не найдено
#Ожидаемый:    подходящих багов не найдено

#ВЫВОД: с Few-shot модель дала верный ответ по кейсу, который провалила без них. Есть кейсы с похожими словами, но разным смыслом, ей нужно показать разницу.